#include<bits/stdc++.h>
using namespace std;

int main(){
    int visited;
    vector<int>v( visited , 0);
    int mini = min(2,3);
    cout << mini << endl;
    int CMCPP = min({0,2,3,4,5});
    cout << CMCPP << endl;
/*
    a^b = x; a power b = x
    loga(x) = b; 
*/

// We call two numbers 𝑥 and 𝑦 similar if they have the same parity (the same remainder when divided by 2), or if |𝑥−𝑦|=1. 
}