#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int>v;

    // if values are provided at input

    for(auto value:v){
        v.push_back(value);
    }
    for(auto value:v){
        cout<<value<<" ";
    }
}
